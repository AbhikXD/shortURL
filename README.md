# shortURL
This is a project of Url Shortner
It uses SpringBoot for the backend and Python and HTML frontend for Testing.
Postgresql is used as Database to store the LongURLS and shortURLs.


Docker: This can be run as a Docker container, using the given Dockerfile. Will be attaching the .tar Docker image soon
